import { bootstrapApplication } from '@angular/platform-browser';
import { UserPageComponent } from './app/user-page.component';

bootstrapApplication(UserPageComponent).catch((err) => console.error(err));
